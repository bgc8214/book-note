독서 노트 사이트 PRD
2026년 9월 18일 · @boss
배경과 문제
기술서든 자기계발서든, 한 권을 제대로 읽고 나서 석 달이 지나면 남는 게 거의 없다. 7장에 뭐가 있었는지는커녕 7장이 있었다는 사실조차 기억나지 않는다. 문제는 읽는 행위가 아니라 읽은 것을 다시 꺼내 쓸 수 있는 형태로 남기지 않는다는 것이다.
기존 방법이 안 먹히는 이유는 각각 다르다.
• PDF 검색 — 키워드로 페이지 목록은 나오지만 답은 안 나온다. 저자가 그 개념을 뭐라고 불렀는지 기억해야만 찾을 수 있는데, 정작 기억 안 나는 게 그거다.
• 노트앱에 메모 — 읽으면서 쓴 200줄짜리 문서는 다시 열지 않는다. 구조가 없어서 훑을 수가 없고, 훑을 수 없으니 열 이유가 없다.
• LLM에게 그냥 묻기 — 책 내용을 안 갖고 있으면 그럴듯하게 지어내고, 매번 PDF를 통째로 넣으면 대화 턴마다 비용을 다시 낸다.
이 시스템이 바꾸는 건 하나다. 책 한 권을 다 읽은 직후, 아직 머리에 남아 있을 때 한 번만 구조화 비용을 치르고, 그 결과를 사이트에 영구히 쌓는다. 이후에는 PDF를 다시 열 일이 없다. 챕터별로 핵심 아이디어와 실천 아이템이 정리된 페이지를 보면 되고, 그 페이지는 검색 가능하고 링크 가능하며 시간이 지나도 사라지지 않는다.
부수적으로 공개 사이트라는 점이 품질 강제 장치가 된다. 남이 볼 수 있는 글은 대충 쓰기 어렵다.
목표와 비목표
목표
1. 책 한 권을 한 번의 명령으로 사이트에 올린다. PDF 경로를 주고 로컬 에이전트에게 시키면, 스키마에 맞는 마크다운 파일들이 생성되고 커밋 가능한 상태가 된다.
2. 챕터 단위로 다시 꺼내 쓸 수 있다. 각 챕터 페이지에 그 장의 핵심 아이디어, 요점, 실천 아이템, 용어가 정리되어 있다.
3. 책이 쌓여도 형태가 일관된다. 10권째 책이 1권째와 같은 구조로 렌더링된다.
4. 공개해도 문제없는 수준의 글이다. 원문 복제가 아니라 내 정리라는 게 형태상 분명하다.
비목표
• 읽기 대체가 아니다. 안 읽은 책을 요약시키는 도구가 아니라, 읽은 책을 남기는 도구다. 이 구분이 흐려지면 품질과 저작권 양쪽이 무너진다.
• 전문(全文) 보관이 아니다. 원문 텍스트는 사이트에 올라가지 않는다. 추출된 원문은 로컬 중간 산출물로만 존재하고 저장소에 커밋하지 않는다.
• 책 검색 엔진이 아니다. 여러 권을 가로질러 질의하는 건 v1 범위 밖이다.
• 자동화 100%가 아니다. 에이전트가 초안을 만들고 내가 검수 후 커밋한다. 검수 없는 자동 게시는 하지 않는다.
• 댓글, 좋아요, 구독 같은 사이트 기능은 다루지 않는다. 이 PRD는 콘텐츠 파이프라인만 정의한다.
사용자 워크플로우
책을 다 읽은 직후, 기억이 아직 살아 있을 때 한 번에 처리한다. 전체 소요 시간 목표는 검수 포함 30분 이내.
1. PDF를 작업 폴더에 둔다. ~/books/inbox/atomic-habits.pdf 같은 식.
2. 에이전트에게 한 줄 지시한다.
   ~/books/inbox/atomic-habits.pdf 읽고 사이트 스키마에 맞춰 초안 만들어줘
3. 에이전트가 처리한다. 추출 → 챕터 분할 → 구조화 → 스키마대로 마크다운 생성. 결과는 사이트 저장소의 브랜치에 올라간다.
4. 내가 검수한다. 여기가 핵심 단계다. 에이전트는 책을 읽었지만 나처럼 읽지는 않았다. 실천 아이템이 일반론으로 뭉개졌거나, 저자가 강조한 걸 놓쳤거나, 내가 중요하게 본 대목이 빠졌으면 이때 고친다. 읽으면서 든 내 생각을 덧붙이는 것도 여기서 한다.
5. 커밋하고 배포한다. 로컬 프리뷰로 렌더링 확인 후 머지.
검수를 필수로 두는 이유
자동 게시를 안 하는 건 품질 문제이기도 하지만 더 큰 이유가 있다. 검수 단계가 없으면 이 사이트는 "내가 읽은 책의 정리"가 아니라 "AI가 요약한 책 목록"이 된다. 전자는 내 저작물이고 후자는 남의 책의 파생물에 가깝다. 공개 사이트에서는 이 차이가 실질적으로 중요하다.
중단 조건
• 스캔 PDF(텍스트 레이어 없음)면 추출 단계에서 즉시 중단하고 OCR을 먼저 돌리라고 알린다.
• 챕터 경계를 못 잡으면 진행하지 않고 목차 구조를 물어본다. 잘못 나뉜 챕터로 진행하면 검수 비용이 처음부터 다시 하는 것보다 커진다.
콘텐츠 스키마
이 문서에서 가장 중요한 절이다. 스키마가 곧 에이전트에게 주는 계약이고, 동시에 사이트 렌더링 컴포넌트의 입력 형식이다. 책 5권이 쌓인 뒤에 필드를 바꾸면 앞의 것을 전부 손봐야 하므로, 여기서 정한 걸 초반에 고정한다.
파일 배치
content/books/<book-slug>/
  index.md              # 책 메타 + 한 줄 총평 + 챕터 목록
  ch01-<chapter-slug>.md
  ch02-<chapter-slug>.md
  ...
  glossary.md           # 이 책의 용어를 한곳에 모은 페이지
book-slug는 영문 소문자 케밥케이스. 한국어 책도 로마자로 옮긴 슬러그를 쓴다(mimeticdesire 같은 임의 축약보다 원제 기준이 나중에 찾기 쉽다). 챕터 파일명의 번호는 정렬용이고, 실제 표시 순서는 프론트매터의 order를 따른다.
책 프론트매터 (index.md)
---
title: 아주 작은 습관의 힘
original_title: Atomic Habits
author: James Clear
translator: 이한이
publisher: 비즈니스북스
year: 2019
slug: atomic-habits
cover: /covers/atomic-habits.jpg
read_at: 2026-09-14        # 다 읽은 날
rating: 4                  # 1-5, 내 평가
tags: [습관, 자기계발, 행동설계]
one_liner: 의지가 아니라 환경과 시스템을 바꾸라는 주장을 4단계 루프로 정리한 책
---
챕터 프론트매터
---
book: atomic-habits
order: 3
title: 습관을 바꾸는 네 가지 법칙
slug: four-laws
---
챕터 본문 섹션
순서와 헤딩 이름을 고정한다. 에이전트가 이 형태로만 쓰게 하고, 렌더링 컴포넌트도 이걸 전제한다.
섹션
형식
분량 기준
비고
## 핵심 아이디어
산문
2-3문장
이 장을 한 문단으로 줄이면 무엇인가
## 요점
불릿
3-6개
각 항목은 한 문장. 주장 단위로 끊는다
## 실천 아이템
체크리스트
2-5개
아래 규칙 참고
## 용어
정의 목록
0-8개
이 장에서 처음 나온 개념만
## 연결
불릿
0-3개
다른 챕터·다른 책으로의 내부 링크
비어 있는 섹션은 헤딩째 생략한다. 모든 장에 실천 아이템이 있는 건 아니다(도입부, 배경 설명 장 등). 빈 섹션을 억지로 채우면 그 장부터 신뢰도가 떨어진다.
실천 아이템 작성 규칙
이 사이트의 핵심 가치가 여기 걸려 있으므로 규칙을 따로 둔다.
• 관찰 가능한 행동으로 쓴다. "환경을 설계한다"(X) → "운동복을 전날 밤 침대 옆에 꺼내둔다"(O)
• 책의 주장에서 직접 도출되어야 한다. 일반적인 좋은 조언은 넣지 않는다. 그 책을 안 읽어도 쓸 수 있는 문장이면 삭제 대상이다.
• 내 상황에 맞춘 것은 검수 때 내가 쓴다. 에이전트는 책이 제안한 것까지만 쓰고, 개인화는 사람 몫이다.
용어집 병합
챕터별 ## 용어는 그 장에 남고, 동시에 glossary.md에 가나다순으로 누적된다. 같은 용어가 여러 장에 나오면 정의는 처음 나온 장 것을 쓰고 나머지 장 링크만 덧붙인다. 에이전트는 기존 glossary.md를 먼저 읽고 머지해야 하며, 덮어쓰지 않는다.
파이프라인
네 단계고, 앞 두 단계는 기계적, 뒤 두 단계는 판단이 들어간다.
1단계 — 추출
입력: PDF/EPUB 파일 경로. 출력: 정제된 텍스트 + 챕터 경계 + 메타데이터.
직접 구현하지 않고 book-to-skill의 scripts/extract.py를 가져다 쓴다. MIT 라이선스이고 스킬 설치 없이 단독 CLI로 동작한다. 이 선택의 근거:
• 포맷별로 가용한 도구를 순서대로 시도하는 폴백 체인이 이미 구현되어 있다(PDF는 pdftotext → pypdf → pdfminer, 기술서는 docling).
• 스캔 PDF를 첫 페이지 검사로 걸러내고 즉시 중단한다. 직접 짜면 빠뜨리기 쉬운데, 없으면 책 한 권을 다 돌리고 빈 결과물을 받는다.
• 챕터 경계 탐지가 들어 있다.
• 결정론적 파이썬 코드라 LLM 호출 비용이 없다.
python3 scripts/extract.py --check로 설치된 추출기를 확인할 수 있다. 우리 용도(주로 산문)에는 poppler의 pdftotext면 충분하고, 표·코드가 많은 기술서일 때만 docling을 쓴다.
다만 book-to-skill의 생성기 쪽(SKILL.md)은 쓰지 않는다. 그쪽 산출물은 에이전트가 온디맨드로 로드하는 스킬 폴더 형태이고 목적지가 우리 사이트와 다르다. 스킬 폴더를 만든 뒤 다시 우리 스키마로 변환하는 건 한 단계가 통째로 낭비이고 그 과정에서 압축 손실만 생긴다.
2단계 — 구조화
입력: 챕터별 텍스트. 출력: 챕터별로 위 스키마의 다섯 섹션이 채워진 초안.
이 단계가 LLM이 실제로 판단하는 유일한 지점이다. 챕터를 하나씩 처리하고, 한 챕터의 결과를 다음 챕터 처리 시 컨텍스트로 넘기지 않는다(용어 중복 판정만 예외). 책 전체를 한 번에 넣으면 뒤로 갈수록 뭉개진다.
3단계 — 파일 생성
입력: 구조화 결과. 출력: 스키마대로 된 마크다운 파일들 + 기존 glossary.md 머지.
기계적인 단계지만 두 가지를 지켜야 한다. 기존 파일을 덮어쓰지 않을 것, 그리고 슬러그 충돌을 확인할 것.
4단계 — 검수와 커밋
사람이 한다. 브랜치에 올라온 초안을 읽고 고친 뒤 머지한다.
중간 산출물 처리
1단계의 추출 원문은 .gitignore에 넣어 저장소에 들어가지 않게 한다. 로컬 작업 폴더에만 두고, 게시 후에는 아카이브 폴더로 옮겨 보관한다. 원문 전문이 공개 저장소 히스토리에 한 번이라도 커밋되면 되돌리기 번거롭고, 저작권 측면에서도 가장 피해야 할 형태다.
에이전트 질의
사이트는 사람이 읽으려고 만들지만, 콘텐츠가 저장소의 마크다운이므로 로컬 에이전트가 그대로 읽을 수 있다. 부수 효과가 아니라 의도된 이중 용도다.
답할 수 있는 것과 없는 것
스키마를 고정했기 때문에 모든 책에서 헤딩 이름이 같고, 에이전트가 섹션을 정확히 집어낼 수 있다. 다음은 잘 된다.
• 특정 장의 실천 아이템, 요점, 용어 조회
• 프론트매터 기반 필터링 (태그, 읽은 시기, 평점)
• 용어 정의와 그 용어가 나온 장 찾기
• 여러 책에 걸친 태그 검색
답할 수 없는 것은 내가 안 적어둔 전부다. 에이전트가 참조하는 건 책이 아니라 내 정리이므로, 요점 5개로 줄인 장에 대해 저자가 인용한 연구의 표본 수를 물으면 답이 없다. 이건 설계상 감수한 트레이드오프다 — 사이트에 원문을 올리지 않기로 한 결정의 필연적 결과.
원문 아카이브
위 한계를 완화하기 위해, 추출된 원문을 커밋하지는 않되 로컬에는 남긴다.
~/books/archive/<book-slug>/
  raw.txt          # extract.py 출력
  chapters/        # 챕터별 분할 텍스트
저장소 밖의 경로이므로 공개되지 않고 저작권 문제도 없다. 원문까지 파고들 일이 생기면 그때 이 폴더를 에이전트에게 열어주면 된다. 파이프라인 절의 "게시 후 삭제"는 이 규칙으로 대체한다.
인덱스 파일
책이 쌓이면 파일 수가 빠르게 는다. 10권에 권당 30장이면 300개가 넘고, 질문마다 전체를 훑는 건 느리고 비싸다. 에이전트가 먼저 읽을 목차를 하나 둔다.
content/index.md — 책 목록, 각 책의 챕터 제목과 한 줄 요약, 파일 경로만 담는다. 본문은 없다. 에이전트는 이걸 읽고 어느 파일을 열지 정한 뒤 그 파일만 연다.
## 아주 작은 습관의 힘 (James Clear, 2019) — atomic-habits
태그: 습관, 자기계발 · 읽은 날: 2026-09-14

- 3장 습관을 바꾸는 네 가지 법칙 — 신호·열망·반응·보상 루프를 뒤집어 나쁜 습관을 없애는 법 → `books/atomic-habits/ch03-four-laws.md`
- 4장 ...
3단계(파일 생성)에서 자동으로 갱신한다. 책 하나를 올릴 때 해당 책 블록만 추가하거나 교체하고, 다른 책 블록은 건드리지 않는다.
질의 예시
습관 관련해서 내가 읽은 책들에서 실천 아이템만 다 뽑아줘
아주 작은 습관의 힘 3장 요점만 다시 보여줘
'신호'라는 용어가 나온 장 전부 찾아줘
작년에 읽은 책 중 평점 4 이상인 것 목록
이런 질의가 안정적으로 동작하려면 인덱스 파일이 최신이어야 한다. 검수 단계 체크리스트에 인덱스 갱신 확인을 넣는다.
저작권과 공개 범위
전체 공개로 가기로 했으므로 이 절이 제약 조건이 된다. 비공개였다면 신경 쓸 필요가 거의 없는 부분이다.
위험의 성격
저작권 있는 책을 챕터별로 상세히 정리한 글을 공개하는 건 회색지대다. 한쪽 끝에는 서평이나 독후감이 있고(문제없음), 다른 쪽 끝에는 원문을 장별로 옮긴 것이 있다(명백한 침해). 챕터별 요약은 그 사이 어딘가에 있고, 상세할수록 위험한 쪽으로 간다. 원본 구매를 대체할 정도가 되면 선을 넘는다.
참고로 book-to-skill 저장소도 같은 경고를 한다. 생성물이 원문 복제가 아닌 구조화된 파생물이더라도 저작권 있는 책의 스킬을 공개·배포하면 권리자 침해가 될 수 있으니 비공개로 두라는 입장이다.
스키마에 반영하는 규칙
앞 절의 스키마는 이 위험을 형태로 낮추도록 설계되어 있다.
• 원문 발췌 금지. 인용은 장당 한 문장 이내, 큰따옴표와 출처를 붙인다. 발췌를 이어 붙여 원문을 재구성하지 않는다.
• 장 요약이 아니라 장 소화. ## 요점은 줄거리 재현이 아니라 주장 단위 정리다. 책의 서술 순서를 그대로 따라가지 않는다.
• 실천 아이템은 내 해석이다. 저자의 주장에서 내가 도출한 행동이고, 이 섹션이 사이트에서 가장 원저작물성이 높은 부분이다. 비중을 키울수록 안전해지고 동시에 유용해진다.
• 분량 상한을 둔다. 챕터 페이지가 길어질수록 대체재가 된다. 위 표의 항목 수 상한이 그 역할을 한다.
• 원문 텍스트는 커밋하지 않는다. 파이프라인 절의 중간 산출물 처리 규칙.
표시
각 책 페이지 하단에 이 글이 개인 독서 기록이며 원서를 대체하지 않는다는 문구와 함께 원서 구매 링크를 둔다. 법적 면책은 아니지만 의도를 분명히 하고, 실제로 독자를 원본으로 보내는 역할을 한다.
판단이 필요한 경우
소설이나 서사 중심 책은 애초에 이 형식에 안 맞고(실천 아이템이 나올 게 없다) 줄거리 요약은 스포일러이자 대체재라 위험도 높다. 이 사이트에는 올리지 않는 쪽이 낫다.
이 문서는 법률 자문이 아니다. 공개 규모가 커지거나 수익화가 붙으면 그때는 전문가에게 확인하는 게 맞다.
v1 범위와 이후
v1에 만드는 것
1. 스키마 확정 (이 문서의 콘텐츠 스키마 절)
2. 에이전트 지시서 — 저장소에 두는 마크다운 한 장. 스키마, 파일 경로 규칙, 작성 규칙, 중단 조건을 담는다. 이게 있어야 매번 같은 결과가 나온다.
3. 사이트 렌더링 — 책 목록 페이지, 책 페이지, 챕터 페이지, 용어집 페이지
4. 책 한 권으로 끝까지 관통 (검증)
5. 인덱스 파일 content/index.md 생성과 자동 갱신
나중으로 미루는 것
• 전체 검색 — 책이 5권은 쌓여야 의미가 있다. 정적 사이트면 Pagefind 같은 걸로 나중에 붙이면 된다.
• 태그 브라우징 — 프론트매터에 tags는 지금부터 넣되, 태그 페이지는 나중에. 데이터만 쌓아두면 된다.
• 책 간 개념 연결 — 같은 개념이 여러 책에 나올 때 엮는 기능. 가장 탐나는 기능이지만 가장 나중이다. 책이 적을 때 만들면 연결할 게 없어서 잘 만들었는지 판단도 안 된다.
• 읽는 중 상태 관리 — 다 읽고 한 번에 올리는 흐름이므로 v1에는 불필요하다.
첫 책 선정 기준
검증용 첫 책은 이미 잘 아는 책, 챕터 구조가 뚜렷한 책, 텍스트 레이어 있는 PDF로 고른다. 결과물이 제대로 나왔는지 판단하려면 내가 그 책을 알아야 한다. 어려운 책으로 시작하면 파이프라인이 문제인지 책이 문제인지 구분이 안 된다.
열린 질문
• 한국어 번역서에서 용어를 원어와 병기할 것인가 (예: 신호 (cue))
• 챕터가 30개 넘는 책을 어떻게 다룰 것인가 — 파일 30개 vs 부(part) 단위 묶음
• 같은 책을 재독했을 때 기존 페이지를 갱신할 것인가, 버전을 나눌 것인가