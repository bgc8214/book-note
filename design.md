# Design — 독서 노트

이 앱의 잠긴 디자인 시스템이다. 페이지를 고치기 전에 이 파일을 먼저 읽는다.
페이지마다 새로 만들지 말고, 시스템이 자라야 할 때 이 파일을 고친다.

## Genre

editorial

읽기가 목적인 사이트다. 설득하거나 전환시키지 않는다. 화면은 종이처럼 굴고,
장식은 활자와 괘선으로만 한다.

## Macrostructure family

페이지 종류마다 형태가 다르되 테마·활자·간격은 공유한다.

- **본문 페이지** (챕터, 용어집) — **Long Document**. 연속된 산문에 인라인 섹션 헤드.
  좌측 본문 + 우측 섹션 목차. 변주 가능한 축: 목차 유무.
- **목록 페이지** (책 목록, 책 페이지) — **Index-First**. 페이지 자체가 링크의 목록이다.
  히어로 없음. 변주 가능한 축: 표지 썸네일 유무, 정렬 컨트롤 유무.
- **리뷰 페이지** (`/today`) — **Catalogue**. 같은 종류의 조각들이 균일한 격자로.

## Theme

**Almanac** — 연감. 기록이 쌓이는 곳의 색.

라이트

- `--color-paper`      oklch(97% 0.008 75)
- `--color-paper-2`    oklch(94.5% 0.010 75)
- `--color-paper-3`    oklch(91% 0.013 70)
- `--color-rule`       oklch(86% 0.011 70)
- `--color-neutral`    oklch(52% 0.012 65)   (보조 텍스트 4.5:1 확보)
- `--color-muted`      oklch(46% 0.014 60)
- `--color-ink`        oklch(22% 0.014 55)
- `--color-accent`     oklch(48% 0.13 50)
- `--color-accent-ink` oklch(97% 0.008 75)
- `--color-accent-soft` oklch(93% 0.022 60)
- `--color-focus`      oklch(52% 0.16 50)

다크 — 휴는 고정, 명도와 채도만 움직인다.

- `--color-paper`      oklch(16% 0.010 60)
- `--color-ink`        oklch(93% 0.008 75)
- `--color-neutral`    oklch(64% 0.012 60)
- `--color-accent`     oklch(72% 0.11 55)

## Typography

한국어 사이트라 라틴 전용 페어링을 그대로 쓸 수 없다. 세 벌이 상한이다.

- **Display** — Noto Serif KR, weight 600, style normal. 책 제목과 장 제목.
- **Body** — Pretendard, weight 400 (다크 모드에서 350). 본문.
- **Outlier** — JetBrains Mono, weight 400. 장 번호, 날짜, 콜로폰. **두 자리까지만.**

제목은 언제나 로만. 이탤릭 제목은 금지(전역 규칙).
디스플레이 자간 -0.02em. 본문 행간 1.75 — 한글은 라틴보다 행간이 필요하다.
타입 스케일 1.25(장3도), 본문 17px 기준.

## Spacing

4pt 스케일. 값은 `src/styles/tokens.css` 에 있다. 페이지는 반드시 이름으로 참조한다
(`var(--space-md)`). 날값 금지.

섹션 리듬은 균일하지 않다. 본문 페이지는 위가 좁고 아래가 넓다.

## Motion

- 이징: `--ease-out: cubic-bezier(0.16, 1, 0.3, 1)` 하나만 쓴다.
- 리빌 패턴: **없음.** 스크롤 페이드인을 쓰지 않는다. 내용은 그냥 거기 있다.
- 움직이는 것은 링크 밑줄, 체크 상태, 목차 활성 표시 셋뿐이다.
- `prefers-reduced-motion: reduce` 에서는 150ms 이하 불투명도 전환만 남는다.
- **포커스 링은 절대 전환하지 않는다.** 즉시 나타난다.

## Microinteractions

- 조용한 성공. 토스트 없음.
- 체크 상태는 즉시 반영하고 저장은 뒤에서 한다.
- 호버 전용 기능 금지 — 모든 호버 어포던스에 포커스 상태가 있다.

## CTA voice

- 이 사이트에 1차 CTA는 없다. 무언가를 팔지 않는다.
- 동작 요소(정렬, 다른 5개 보기)는 알약형 아웃라인. 채우지 않는다.
- 링크는 밑줄 색으로 말한다. 버튼처럼 보이지 않게 한다.

## Per-page allowances

- 본문 페이지: 활자만. 장식 금지.
- 목록 페이지: 표지 썸네일 허용. 그 외 이미지 금지.
- 리뷰 페이지: 활자만.

## 모든 페이지가 공유해야 하는 것

- 워드마크와 그 활자.
- 악센트 색과 그 사용량 — **한 화면의 3% 이하.**
- 디스플레이/본문 폰트.
- 섹션 헤드 리듬.
- 포커스 링 모양.

## 페이지마다 달라도 되는 것

- 패밀리 안에서의 매크로구조.
- 목차 유무, 정렬 컨트롤 유무.
- 섹션 간 세로 리듬.

## 하지 않는 것

- 아이브로(`01 · 요점` 같은 섹션 번호표). 콘텐츠가 진짜 순서를 가질 때만.
- 태그-좌측 / 제목-우측 2단 섹션 헤드.
- 카드 안의 카드.
- 그림자로 만드는 높이. 다크 모드에서는 밝기로 올린다.
- 스크롤 페이드인.

## Exports

토큰 원본은 `src/styles/tokens.css` 다. 페이지 CSS 는 `@import` 로 그 파일을 읽고
이름으로만 값을 부른다.

이 프로젝트는 Tailwind, DTCG, shadcn/ui 를 쓰지 않으므로 그 포맷들은 만들지 않았다.
다른 프로젝트로 이 시스템을 옮길 일이 생기면 그때 `tokens.css` 에서 변환한다.
