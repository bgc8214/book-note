# 에이전트 지시서 — 책 한 권을 사이트에 올리기

이 문서가 계약이다. PDF 한 권을 받아 `content/books/<book-slug>/` 아래 마크다운을 만들 때
여기 적힌 형식만 쓴다. 형식을 벗어나면 사이트 렌더링과 `npm run validate` 가 깨진다.

**너는 초안까지만 만든다.** 브랜치에 올려두고 멈춘다. 사람이 검수하고 머지한다.
검수 없는 커밋·머지·배포는 하지 않는다.

---

## 0. 시작 전 확인

책 한 권을 받으면 다음 순서로 진행한다.

1. `content/index.md` 를 읽어 같은 책이 이미 있는지, 슬러그가 겹치지 않는지 본다.
2. 추출 → 챕터 분할 → 구조화 → 파일 생성 순으로 진행한다.
3. 끝나면 `npm run validate && npm run index` 를 돌리고 결과를 보고한다.

### 중단 조건 — 다음 경우 진행하지 말고 사람에게 묻는다

- **스캔 PDF**(텍스트 레이어 없음). 추출 단계에서 즉시 멈추고 OCR 을 먼저 돌리라고 알린다.
- **챕터 경계를 못 잡는 경우.** 추측해서 나누지 않는다. 목차 구조를 물어본다.
  잘못 나뉜 챕터로 진행하면 검수 비용이 처음부터 다시 하는 것보다 커진다.
- **소설·서사 중심 책.** 이 형식에 맞지 않는다(실천 아이템이 나오지 않고, 줄거리 요약은
  스포일러이자 원본 대체재다). 올리지 않는 쪽이 낫다고 알린다.
- 프론트매터에 필요한 서지 정보(저자, 출판사, 연도)를 본문에서 확정할 수 없는 경우.

---

## 1. 추출

book-to-skill(MIT)의 `extract.py` 를 쓴다. 직접 파싱하지 않는다. 그 저장소는 이 저장소에
복제하지 않고 `~/books/tools/book-to-skill` 에 따로 두며, `scripts/extract.sh` 가 감싼다.

```
scripts/extract.sh --check                          # 설치된 추출기 확인
scripts/extract.sh ~/books/inbox/<file>.pdf <book-slug>
scripts/extract.sh ~/books/inbox/<file>.pdf <book-slug> --mode technical
```

- 산문 위주 책이면 poppler `pdftotext` 로 충분하다(설치돼 있다). 표·코드가 많은 기술서일 때만
  `--mode technical`(docling 필요).
- 출력은 `~/books/archive/<book-slug>/raw.txt`. 챕터로 나눈 텍스트는 같은 폴더의
  `chapters/` 에 둔다.
- **추출 원문은 저장소에 절대 커밋하지 않는다.** 원문이 공개 저장소 히스토리에 한 번이라도
  들어가면 되돌리기 어렵고, 저작권 측면에서 가장 피해야 할 형태다.
- 아카이브는 지우지 않고 남긴다. 나중에 원문까지 파고들 일이 생기면 그 폴더를 열어주면 된다.

## 2. 구조화

챕터를 **하나씩** 처리한다. 한 챕터의 결과를 다음 챕터 처리 시 컨텍스트로 넘기지 않는다.
예외는 용어 중복 판정뿐이다. 책 전체를 한 번에 넣으면 뒤로 갈수록 내용이 뭉개진다.

## 3. 파일 생성

```
content/books/<book-slug>/
  index.md              # 책 메타 + 총평 + (챕터 목록은 사이트가 자동 생성)
  ch01-<chapter-slug>.md
  ch02-<chapter-slug>.md
  glossary.md
```

- `book-slug` 는 영문 소문자 케밥케이스. 한국어 책도 **원제 기준** 로마자 슬러그를 쓴다.
  임의 축약 금지 — 나중에 찾기 어렵다.
- 파일명의 번호는 정렬용이고, 실제 표시 순서는 프론트매터 `order` 를 따른다. 둘은 일치시킨다.
- **기존 파일을 덮어쓰지 않는다.** 슬러그가 겹치면 멈추고 묻는다.

---

## 4. 스키마

### 책 프론트매터 (`index.md`)

```yaml
---
title: 아주 작은 습관의 힘        # 필수
original_title: Atomic Habits
author: James Clear               # 필수
translator: 이한이
publisher: 비즈니스북스
year: 2019
slug: atomic-habits               # 필수, 디렉토리명과 같아야 한다
cover: /covers/atomic-habits.jpg
read_at: 2026-09-14               # 필수, 다 읽은 날
rating: 4                         # 필수, 1-5
tags: [습관, 자기계발, 행동설계]
one_liner: 의지가 아니라 환경과 시스템을 바꾸라는 주장을 4단계 루프로 정리한 책  # 필수
buy_url: https://...              # 원서 구매 링크, 있으면
---
```

`index.md` 본문에는 책 전체에 대한 짧은 총평만 쓴다. 챕터 목록은 사이트가 자동으로 붙인다.

### 챕터 프론트매터

```yaml
---
book: atomic-habits               # 책 slug 과 같아야 한다
order: 3                          # 장 번호
title: 습관을 바꾸는 네 가지 법칙
slug: four-laws                   # URL 이 된다. 'glossary' 는 쓸 수 없다
---
```

### 챕터 본문 — 섹션 순서와 이름 고정

| 섹션              | 형식        | 분량      | 비고                                    |
| ----------------- | ----------- | --------- | --------------------------------------- |
| `## 핵심 아이디어` | 산문        | 2-3문장   | 필수. 이 장을 한 문단으로 줄이면 무엇인가 |
| `## 요점`          | 불릿        | 3-6개     | 각 항목 한 문장. 주장 단위로 끊는다       |
| `## 실천 아이템`   | 체크리스트  | 2-5개     | 아래 규칙 참고                           |
| `## 용어`          | 정의 목록   | 0-8개     | 이 장에서 처음 나온 개념만                |
| `## 연결`          | 불릿        | 0-3개     | 다른 챕터·다른 책으로의 내부 링크         |

- 순서를 바꾸지 않는다. 다른 헤딩 이름을 만들지 않는다. `###` 하위 헤딩을 넣지 않는다.
- **비어 있는 섹션은 헤딩째 생략한다.** 모든 장에 실천 아이템이 있는 것은 아니다(도입부,
  배경 설명 장). 빈 섹션을 억지로 채우면 그 장부터 신뢰도가 떨어진다.
- `## 핵심 아이디어` 의 첫 문장은 인덱스와 챕터 목록의 한 줄 요약으로 그대로 쓰인다.
  이 문장만 읽어도 장의 내용을 알 수 있게 쓴다.

형식 예:

```markdown
## 요점

- 습관은 신호·열망·반응·보상의 네 단계 루프로 작동한다.

## 실천 아이템

- [ ] 운동복을 전날 밤 침대 옆에 꺼내둔다.

## 용어

신호 (cue)
: 행동을 촉발하는 자극.
```

정의 목록은 **용어 줄 다음 줄에 `: ` 로 시작하는 정의**를 쓴다. 이 형식이어야 `<dl>` 로 렌더된다.

### 실천 아이템 작성 규칙

이 사이트의 가치가 여기 걸려 있다.

- **관찰 가능한 행동으로 쓴다.** "환경을 설계한다"(X) → "운동복을 전날 밤 침대 옆에 꺼내둔다"(O)
- **책의 주장에서 직접 도출되어야 한다.** 그 책을 읽지 않고도 쓸 수 있는 문장이면 삭제 대상이다.
- **개인화는 하지 않는다.** 책이 제안한 것까지만 쓴다. 내 상황에 맞춘 것은 사람이 검수 때 쓴다.

### 용어집 (`glossary.md`)

```yaml
---
book: atomic-habits
---
```

- 챕터별 `## 용어` 는 그 장에 남기고, 동시에 `glossary.md` 에 **가나다순으로 누적**한다.
- 같은 용어가 여러 장에 나오면 정의는 **처음 나온 장** 것을 쓰고, 나머지 장은 링크만 덧붙인다.
- **기존 `glossary.md` 를 먼저 읽고 머지한다. 덮어쓰지 않는다.**

---

## 5. 저작권 — 형식으로 지키는 선

전체 공개 사이트다. 아래는 취향이 아니라 제약이다.

- **원문 발췌 금지.** 인용은 장당 한 문장 이내, 큰따옴표와 출처를 붙인다. 발췌를 이어 붙여
  원문을 재구성하지 않는다.
- **장 요약이 아니라 장 소화.** `## 요점` 은 줄거리 재현이 아니라 주장 단위 정리다.
  책의 서술 순서를 그대로 따라가지 않는다.
- **분량 상한을 지킨다.** 챕터 페이지가 길어질수록 원본 대체재가 된다. 위 표의 항목 수 상한이
  그 역할을 한다.
- **원문 텍스트는 커밋하지 않는다.**

## 6. 마무리

```
npm run validate              # 섹션 구성·항목 수·슬러그 충돌 검사
npm run index                 # content/index.md 갱신 (--book <slug> 로 한 권만도 가능)
npm run build                 # validate + index + astro build
```

보고할 것: 만든 파일 목록, validate 결과, 판단이 갈렸던 지점(챕터 경계, 실천 아이템을 넣지
않은 장, 용어 중복 처리). 사람은 그 지점부터 읽는다.

---

## 저장소 메모

- 콘텐츠는 `src/content/` 가 아니라 저장소 루트의 `content/` 에 있다. 스키마는
  `src/content.config.ts`.
- 개발 서버는 백그라운드로 띄운다: `astro dev --background`
  (`astro dev stop` / `status` / `logs`).
- Astro 문서: https://docs.astro.build — [콘텐츠 컬렉션](https://docs.astro.build/en/guides/content-collections/),
  [라우팅](https://docs.astro.build/en/guides/routing/)
