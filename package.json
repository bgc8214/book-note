{
  "name": "book-note",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate && npm run index && astro build",
    "preview": "astro preview",
    "validate": "node scripts/validate-content.mjs",
    "index": "node scripts/build-index.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/markdown-satteri": "^0.4.1",
    "astro": "^7.3.3"
  },
  "allowScripts": {
    "esbuild": true
  }
}
